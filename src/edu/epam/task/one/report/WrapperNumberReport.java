package edu.epam.task.one.report;

import edu.epam.task.one.entity.WrapperNumber;

public class WrapperNumberReport {
    public void printReport(WrapperNumber inputNumber, WrapperNumber result) {
        System.out.println(inputNumber + "\nLast digit of num's last digit in second power : " + result);
    }
}
